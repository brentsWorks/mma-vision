"""Position ontology + transition grammar — the load-bearing spec.

States (Tier 0-3, dual-indexed top/bottom), the transition cost matrix, and the
loaders that feed the constrained decoder. See docs/ontology.md.
"""
