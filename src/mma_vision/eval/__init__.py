"""Eval harness.

Tier/bucket-layered metrics (incl. confident-wrong rate, swap-detection recall,
calibration/ECE), versioned frozen held-out eval set, multi-cam answer key, and the
per-bucket regression CI gate. Also IS the Tier-1 probe. See docs/workstreams.md WS3.
"""
