"""Perception core.

Regime detection, identity-memory (SAM2-class), the joint keypoint+identity head,
contact topology, and the identity-swap DETECTOR. Emits calibrated uncertainty —
honest degradation beats fragile precision. See DESIGN.md §1.
"""
